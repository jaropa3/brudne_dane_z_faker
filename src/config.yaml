database:
  host: localhost
  port: 5432
  DB_name: postgres
  sslmode: require

paths:
  raw_data: data/raw_data
  quarantine: logs/
files:
  customers: customers.csv
  orders: orders.csv
  products: products.csv
output:
  raport_name: raport_rekordow_odrzuconych